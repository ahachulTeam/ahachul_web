// import { useState } from 'react';

// import type { EditableImage } from '@/types';

// const useFormImage = (initImages: EditableImage[]) => {
//   const [images, setImages] = useState<EditableImage[]>(initImages ?? []);
//   const [removeImageIds, setRemoveImageIds] = useState<number[]>([]);

//   return { images, setImages, removeImageIds, handleImgChange, handleDeleteImg };
// };

// export default useFormImage;
