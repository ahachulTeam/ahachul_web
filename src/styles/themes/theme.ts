import { input, tab, tag, button } from "./components";
import { a11y, boxShadows, breakPoint, colors, fonts, size, zIndex } from "./foundations";

export const theme = {
  a11y,
  boxShadows,
  breakPoint,
  colors,
  fonts,
  input,
  tab,
  zIndex,
  size,
  tag,
  button,
};
