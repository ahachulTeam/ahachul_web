import { input, toggle, badge, button, checkbox } from "./components";
import {
  a11y,
  animations,
  boxShadows,
  breakPoint,
  colors,
  fonts,
  size,
  zIndex,
} from "./foundations";

export const theme = {
  a11y,
  animations,
  boxShadows,
  breakPoint,
  colors,
  fonts,
  input,
  toggle,
  zIndex,
  size,
  badge,
  button,
  checkbox,
};
