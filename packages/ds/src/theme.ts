import { a11y, animations, boxShadows, breakPoint, colors, fonts, size, zIndex, common } from './tokens'

export const theme = {
  a11y,
  animations,
  boxShadows,
  breakPoint,
  colors,
  fonts,
  zIndex,
  size,
  common,
}
